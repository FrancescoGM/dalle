{
  "name": "@dalle/api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "set NODE_ENV=production&&node dist",
    "build": "tsc --build",
    "dev": "set NODE_ENV=dev&&ts-node-dev --respawn --transpile-only --ignore-watch node_modules src/index.ts"
  },
  "dependencies": {
    "@grpc/proto-loader": "^0.5.6",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "grpc": "^1.24.5",
    "ts-node-dev": "^1.1.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.10",
    "@types/express": "^4.17.11"
  }
}